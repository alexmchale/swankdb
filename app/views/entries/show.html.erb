<p>
  <b>Content:</b>
  <%=h @entry.content %>
</p>

<p>
  <b>Tags:</b>
	<%= link_entry_tags @entry %>
</p>

<p>
	<b>URLs:</b>

	<% @entry.each_filter_item(:url) do |item, url| %>
		<%= link_to item, url %>
	<% end %>
</p>

<p>
	<b>UPS:</b>

	<% @entry.each_filter_item(:ups) do |item, url| %>
		<%= link_to item, url %>
	<% end %>
</p>

<p>
	<b>FedEx:</b>

	<% @entry.each_filter_item(:fedex) do |item, url| %>
		<%= link_to item, url %>
	<% end %>
</p>

<%= link_to 'Edit', edit_entry_path(@entry) %> |
<%= link_to 'Back', entries_path %>